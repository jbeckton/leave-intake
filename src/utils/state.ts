/**
 * Shared state type definitions for agents
 *
 * This file will contain state interfaces used across multiple agents.
 * For now, agents use the default LangChain state, but custom state
 * can be defined here as the project grows.
 */

/**
 * Example: Leave intake state
 * Uncomment and modify when implementing leave intake agents
 */
// export interface LeaveIntakeState {
//   employeeId?: string;
//   leaveType?: string;
//   startDate?: string;
//   endDate?: string;
//   reason?: string;
//   currentStep?: string;
//   validationErrors?: string[];
// }
