{
  "name": "leave-intake-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "agents:studio": "pnpm --filter @leave-intake/agents studio",
    "agents:typecheck": "pnpm --filter @leave-intake/agents typecheck",
    "agents:lint": "pnpm --filter @leave-intake/agents lint",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
